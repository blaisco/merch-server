<p><%= form.label :name %><br/>
<%= form.text_field :name %></p>

<p><%= form.label :parent_id, "Parent Category" %><br/>
<%= form.select :parent_id, options_for_select(ProductType.all.map { |cat| [cat.name, cat.id] }, @category.parent_id)%></p>

<p><%= form.label :rank %><br/>
<%= form.text_field :rank %></p>
