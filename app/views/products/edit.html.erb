<h1><%= @title %></h1>

<%= form_for @product do |f| %>
  <%= errors_for(@product, "We were unable to update that product.") %>
  <%= render :partial => "form", :object => f %>
  <%= submit_tag_with_image "Update", "/assets/icons/tick.png", "positive button" %>
<% end %>
